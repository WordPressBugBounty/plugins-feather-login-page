<?php
if ( ! defined( 'ABSPATH' ) ) exit;

define( 'FPOLPB_PLUGIN_PATH', plugin_dir_path( __FILE__ ) );

define( 'FPOLPB_PLUGIN_URL', plugins_url( '',plugin_basename( __FILE__ ) ) );

define( 'FPOLPB_PLUGIN_DIR_NAME', dirname( plugin_basename( __FILE__ ) ) );

define( 'FPOLPB_PLUGIN_SLUG',  'POLPB' );